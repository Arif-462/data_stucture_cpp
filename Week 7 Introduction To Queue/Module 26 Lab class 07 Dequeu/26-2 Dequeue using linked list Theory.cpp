#include<bits/stdc++.h>
using namespace std;
int main()
{
    /*
        Dequeue not implement singly linked list
        it iplement posssible using doubly linked list
        linked liste thake head and tail

        Push_back()  == push back or laste element insert korar jonno last
        noder pore ekta newnode make korte hobe, last noder next k newnode point kore dite hobe and tail k newnode point kore dite hobe
        newnoder next k null e point kore dite hobe


        Push_frontf() == push front korar jonno head er  previous ke newnode point and newnoder next k heade point korate hobe
        ebar head k null e point korate hobe and all things are keep same;


        pop back() == pop back korar jonno last noder previous ke tail k point korate hobe
        and last noder previous node k null e point korate hobe and delete last node.

        pop front == head er next node head point kora dite hobe and head next node k null point kore dite hobe
        lastly head k delete kore ite hobe

        front() == head->data print kore
        back() == tail->data print kore

        size maintainance kore laste size return kore dibo





    */
    return 0;
}

