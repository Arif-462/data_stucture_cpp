#include<bits/stdc++.h>
using namespace std;
int main()
{
    /*
        Dequeue is double ended queue.
       Dequeue is a super set. and more powerful data structure.
       its perform 4 kinds operation
        1. Pushback() = Backe end element push kore
        2. Pushfront() = fornte e element push kore.
        3. popback() = Last element delete kore
        4. popfront() = first element delete kore

      All the operation perform in O(1).

    */
    return 0;
}
