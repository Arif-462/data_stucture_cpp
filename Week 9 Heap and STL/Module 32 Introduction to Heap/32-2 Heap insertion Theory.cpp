// Theory

Heap == is a complete binary tree
        value store kore rakhe;
        Max heap nia kaj korbo
        Max heap maximum value ber kore
        Max heap = every node value greater or equl from their lower node
        Root is the max number of the heap .

    Min heap == every node value is small or equal of their lower node and
    root is the min nuber node of min heap.

    Insert in max heap = max heap e insert korar somay amra level wise check korbo.
    tar por leve NUll hoile insert korbo. insert korar pore jodi insert value boro hoy tahole
    uporer noder sathe swap kore dite hobe. ei kaj continue korbo max heap
    property jotokhon thik na hoy.

    up-heapify = up-heapify use for call for swap with heap value.

    min heap = imn heap er jonno insert korar somay jodi insert value uporer noder cheye choto hoy thale
    uporer valuer sathe swap kore dite hobe jotokhaon min property thik na hoy
