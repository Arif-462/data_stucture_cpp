#include<bits/stdc++.h>
using namespace std;
int main()
{
    /*
    Graph : a graph in C++ is a non-linear data
            structure defined as a collection of vertices/Node and edges.
            Graph has also a cycle of data structure.

    Vetices = it is the node that is used for storing value.
    Edge = it is the path of one node to another node.





    */
    return 0;
}
