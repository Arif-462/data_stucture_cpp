#include<bits/stdc++.h>
using namespace std;

int main()
{
    /*
    what is stack?
    ans: stack is data structure. many problem solve by this.
    This is pile of object. or stup.

    kaj:

    sobar uporer element ber korte and element add korte pari also remove korte pari;
    sobar nicher elemnt ber and add or remove korte pari'

    sobar nicher elemnt hocce first element mane nich theke suru hoy.

    stack er top and pop operation O(1) kora jay;

    veiw top element kora jay O(1);
    add and delete O(1) kora jay;

    ki kora jay:

    stack jodi array dia implement kora jay tahole random access kora jay
    stack onno kisu dia jamon linked list and other built in function use kora jabe na


    ki ki kaj korajay life:

    expression eveluation like calculator and regular backet sequence kora jay

    stack LIFO method way te kaj kore; last and first out.
    jai element laste add hoy seita firste out hoy.

    */
    return 0;
}
